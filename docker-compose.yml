version: '3.8'

services:

  postgres:
    image: postgres:15
    container_name: loyalty-postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db-init:/docker-entrypoint-initdb.d
    networks:
    - flight-network

  redis:
    image: redis:7
    container_name: loyalty-redis
    restart: always
    ports:
      - "6379:6379"
    networks:
    - flight-network

  auth-service:
    build:
      context: ./auth-service
    container_name: auth-service
    depends_on:
      - postgres
      - redis
    environment:
      - JWT_SECRET=mysupersecurejwtsecretkeyforflight2026project
    networks:
      - flight-network

  api-gateway:
    build:
      context: ./api-gateway
    container_name: api-gateway
    ports:
      - "8081:8081"
    environment:
      - JWT_SECRET=mysupersecurejwtsecretkeyforflight2026project
    depends_on:
      - auth-service
      - redis
    networks:
      - flight-network

networks:
  flight-network:
    driver: bridge

volumes:
  postgres_data: